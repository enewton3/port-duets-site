// import React from "react";
// import { PubNubProvider } from "pubnub-react";
// import { useState } from "react";
// import { pubnub } from "../../services/chatconfig";

export default function PubnubChatWrapper({ children, username }) {
  // const [client] = useState(pubnub(username));
  // console.log(client);
  // return <PubNubProvider client={client}>{children}</PubNubProvider>;
}
