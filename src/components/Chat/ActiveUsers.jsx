import React from "react";

//usePresence to get a new activeUser, push them to the list in state if they arent there already, render that list.
//use their last name as the current channel to go to
export default function ActiveUsers() {
  return <div></div>;
}
